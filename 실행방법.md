# OMR 채점 시스템 — 초보자용 실행 설명서 (A to Z)

컴퓨터를 잘 몰라도 따라 할 수 있게 순서대로 설명합니다.
**딱 3단계**입니다: ①파이썬 설치 → ②프로그램 내려받기 → ③실행 파일 더블클릭.

> 처음 한 번만 준비하면, 다음부터는 **③번(더블클릭)만** 하면 됩니다.

---

## 준비물
- 인터넷이 되는 윈도우 PC 또는 맥
- 시험지 스캔에 쓸 **스캐너(복합기)** — 정확도를 위해 권장

---

# 윈도우(Windows)에서 실행하기

## ① 파이썬(Python) 설치  — 처음 한 번만
1. 크롬/엣지 주소창에 **python.org** 입력 → 접속
2. 상단 메뉴 **Downloads** → 노란 버튼 **Download Python 3.x.x** 클릭
3. 내려받은 파일을 더블클릭해 설치 시작
4. ⚠️ **아주 중요**: 첫 화면 아래쪽 **"Add python.exe to PATH"** 네모칸에 **체크** ✔
5. **Install Now** 클릭 → 끝나면 **Close**

## ② 프로그램 내려받기  — 처음 한 번만
1. 이 프로젝트의 깃허브 주소로 접속합니다:
   `https://github.com/hoonjibae-cmyk/test-report`
2. 화면 왼쪽 위 **브랜치 선택 단추**(보통 `main`이라고 적힌 곳)를 눌러
   **`claude/omr-test-scoring-system-fd34a8`** 를 선택합니다.
   (이미 `main`에 합쳐졌다면 그냥 `main` 그대로 두세요.)
3. 초록색 **Code** 단추 → **Download ZIP** 클릭
4. 내려받은 ZIP 파일을 **바탕화면**에 압축 풀기(오른쪽 클릭 → 압축 풀기)
5. `test-report-...` 라는 폴더가 생깁니다. 이 폴더를 엽니다.

## ③ 실행하기
1. 폴더 안의 **`start_windows.bat`** 파일을 **더블클릭**합니다.
2. 검은 창이 뜨고 "설치 중..." 문구가 나옵니다. **처음엔 몇 분** 걸립니다(기다리세요).
   - "Windows의 PC 보호" 경고가 뜨면 **추가 정보 → 실행**을 누르세요.
3. `http://localhost:8000` 안내가 보이면 준비 완료!
4. **크롬/엣지를 열고 주소창에 `http://localhost:8000` 입력** → 프로그램 화면 등장 🎉

> **끄기**: 검은 창에서 `Ctrl` + `C`. 창을 닫아도 됩니다.
> **다시 켜기**: `start_windows.bat` 를 다시 더블클릭(다음부터는 빨리 켜집니다).

---

# 맥(Mac)에서 실행하기

## ① 파이썬 설치 — 처음 한 번만
1. **python.org** 접속 → **Downloads** → **Download Python 3.x.x**
2. 내려받은 `.pkg` 더블클릭 → 계속/동의 눌러 설치

## ② 프로그램 내려받기 — 처음 한 번만
- 윈도우 ②번과 동일: 깃허브에서 브랜치 선택 → **Code → Download ZIP** → 압축 풀기

## ③ 실행하기
1. 폴더 안 **`start_mac.command`** 파일을 **더블클릭**
   - "확인되지 않은 개발자" 경고가 뜨면: 파일을 **오른쪽 클릭(또는 Control+클릭) → 열기 → 열기**
2. 터미널 창에 "설치 중..." → 잠시 후 `http://localhost:8000` 안내
3. **사파리/크롬 주소창에 `http://localhost:8000` 입력** → 화면 등장 🎉

> **끄기**: 터미널에서 `Control` + `C`.

---

# 프로그램 사용 순서 (화면 안내)

1. **＋ 새 시험 만들기** — 시험 제목, 유형(영어 모의고사/기본), 문항 수 등을 입력
2. **① 답안지(OMR) 출력** — `PDF 다운로드` → **실제 크기(100%)로 인쇄**해 학생에게 배부
   - 인쇄 설정에서 "실제 크기" 또는 "배율 100%" 선택(‘용지에 맞춤’ 해제)
3. 시험 실시 → 학생이 답안지 마킹
4. **② 정답키 입력** — 문항별 정답을 클릭. (영어는 `표준 유형·배점·등급컷 자동 적용` 버튼)
   → **정답키 저장**
5. **③ 스캔 판독·채점** — 마킹된 답안지를 **스캐너로 스캔**(200~300dpi) 후,
   그 이미지 파일들을 `업로드` → 자동으로 점수·석차·등급이 표로 나옵니다
6. **④ 웹 성적표 생성** — 버튼 클릭 → 학생별 **성적표 링크**가 생깁니다(클릭해 열람)

---

# 자주 묻는 문제 (해결법)

- **검은 창에 'py'는 없는 명령…** → 파이썬 설치 때 **PATH 체크**를 안 한 경우입니다.
  파이썬을 지우고 다시 설치하며 ④단계의 **Add to PATH 체크**를 꼭 하세요.
- **'localhost 접속 불가'** → 검은 창(서버)이 켜져 있어야 합니다. 창을 닫았다면 다시 실행.
- **포트가 사용 중이라는 오류** → 이미 켜져 있는 것입니다. 브라우저에서 그냥 접속해 보세요.
- **한글이 네모로 보임(답안지)** → 윈도우/맥에는 기본 한글 글꼴이 있어 보통 문제없습니다.
  문제가 생기면 알려주세요.
- **스캔 대신 휴대폰 사진** → 가능하지만 정면·밝은 곳에서 답안지 전체(네 모서리 마커 포함)가
  나오게 찍어야 정확합니다. 스캐너를 권장합니다.

---

# 참고
- 시험 자료는 프로그램 폴더 안 `data/exams/` 에 저장됩니다(자동).
- 이 방식은 **내 컴퓨터에서만** 접속됩니다. 여러 사람이 인터넷으로 접속하게 하려면
  별도의 서버 배포가 필요합니다(도움 요청 주세요).
