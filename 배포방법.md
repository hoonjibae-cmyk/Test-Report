# 서버 배포 설명서 — 인터넷에서 여러 명이 함께 쓰기 (초보자용)

내 컴퓨터가 아니라 **인터넷 주소(예: `https://우리학원-omr.onrender.com`)** 로 접속해
여러 선생님이 사용하고, 학부모가 성적표 링크를 여는 방식입니다.

> 이 저장소에는 배포에 필요한 파일(`Dockerfile`, `render.yaml`)이 이미 들어 있어,
> **깃허브 연결 → 비밀번호 입력 → 배포** 정도로 끝납니다.

---

## 먼저 알아둘 3가지
1. **비밀번호 보호**: 관리 화면은 로그인해야 열립니다. **학부모 성적표 링크는 로그인 없이 공개**됩니다.
2. **유료(소액)**: 데이터를 잃지 않으려면 **영구 디스크**가 필요하고, 이는 유료 요금제에서 됩니다.
   대략 **월 $7(약 1만원)** 수준부터 시작합니다.
3. **학생 개인정보**: 이름·점수가 저장되므로 비밀번호를 꼭 설정하고, 관리자만 로그인 정보를
   아세요. (국내 서버가 필요하면 아래 '국내 호스팅'을 참고)

---

## 추천: Render.com 으로 배포 (가장 쉬움, 화면 클릭 위주)

### 0단계 — 코드 준비
지금 코드는 `claude/omr-test-scoring-system-fd34a8` 브랜치에 있습니다.
가능하면 이 브랜치를 **`main`으로 병합(Merge)** 해두면 배포가 편합니다.
(깃허브 저장소 → Pull requests → 브랜치 비교 → Merge. 잘 모르면 저에게 요청하세요.)

### 1단계 — Render 가입
1. **render.com** 접속 → **Get Started** → **GitHub 계정으로 로그인**
2. Render가 깃허브 저장소에 접근하도록 **Authorize(허용)**

### 2단계 — 블루프린트로 생성 (자동)
1. Render 대시보드에서 **New +** → **Blueprint**
2. `hoonjibae-cmyk/test-report` 저장소 선택 → Render가 `render.yaml`을 읽어
   웹 서비스와 데이터 디스크를 자동 구성합니다 → **Apply**
3. 배포 도중 **환경변수 입력** 창에서:
   - **OMR_PASSWORD** : 원하는 **관리자 비밀번호** 입력 (예: 학원에서 정한 비번)
   - SECRET_KEY : 자동 생성됨(그대로 두기)
4. **Create / Deploy** 클릭 → 몇 분간 빌드(설치)됩니다.

> 블루프린트가 안 보이면 '수동 방법'(맨 아래)을 쓰세요.

### 3단계 — 접속 & 사용
1. 배포가 끝나면 상단에 **주소(URL)** 가 생깁니다: 예 `https://omr-scoring-xxxx.onrender.com`
2. 그 주소로 접속 → **로그인 화면** → 2단계에서 정한 **OMR_PASSWORD** 입력
3. 이후 사용법은 로컬과 동일: 시험 만들기 → 답안지 출력 → 정답키 → 스캔·채점 → 성적표
4. **학부모에게는** 성적표의 **열람 링크**(예: `.../reports/view/xxxx`)만 전달하면
   로그인 없이 열립니다.

### 4단계 — (선택) 우리 학원 도메인 연결
Render 서비스 **Settings → Custom Domains** 에서 `omr.우리학원.com` 같은 주소를 연결할 수 있습니다.

---

## 비용 정리 (Render 기준, 변동 가능)
- 웹 서비스 **Starter**: 약 **$7/월** — 항상 켜져 있음(무료 요금제는 잠들고 디스크가 없어
  데이터가 사라지므로 실사용 불가).
- 영구 디스크 1GB: 약 **$0.25/월** 수준(성적표 수천 건도 충분).
- 사용량이 늘면 상위 요금제로 올리면 됩니다.

---

## 업데이트(기능 추가) 반영
제가 새 기능을 깃허브에 올리면, Render가 **자동으로 다시 배포**합니다(기본 설정).
수동으로는 Render 대시보드에서 **Manual Deploy → Deploy latest** 를 누르면 됩니다.

---

## 자주 묻는 질문
- **데이터는 어디 저장되나요?** 서버의 영구 디스크(`/data`)에 저장됩니다. 요금제를 유지하는 한
  보존됩니다. 정기 백업이 필요하면 알려주세요(자동 백업도 붙일 수 있습니다).
- **성적표 링크가 안전한가요?** 링크에 **추측 불가능한 긴 코드**가 들어가 있어, 링크를 받은
  사람만 열 수 있습니다. 단, 링크를 아는 사람은 누구나 열 수 있으니 공유에 유의하세요.
- **알림톡으로 링크 보내기**: 배포 후 성적표를 만들면 링크가 그 도메인 기준으로 생성됩니다.
  알림톡 발송(발송대행사 연동)은 다음 단계에서 붙일 수 있습니다.

---

## 국내 호스팅이 필요하면 (개인정보 국내 보관)
학원 규정상 국내 서버가 필요하면 **네이버 클라우드 / 가비아 / 카페24**의
컨테이너·서버 서비스에 같은 `Dockerfile`로 배포할 수 있습니다. 이 경우 설정이 조금 더
복잡하므로, 원하시면 제가 그 환경에 맞춰 단계별로 안내해 드리겠습니다.

---

## (참고) 수동 방법 — 블루프린트 대신
1. Render **New + → Web Service** → 저장소 선택
2. **Runtime: Docker** 자동 인식
3. **Advanced → Add Disk**: Name `omr-data`, Mount Path `/data`, 1GB
4. **Environment** 에 `OMR_PASSWORD`(비밀번호), `SECRET_KEY`(아무 긴 문자열) 추가
5. **Create Web Service** → 배포 완료 후 주소로 접속

---

## 다른 호스팅 (선택지)
- **Railway.app**: Render와 비슷하게 쉬움(Docker 자동 인식). 볼륨으로 `/data` 영구 저장.
- **Fly.io**: 저렴하고 빠르지만 설치 도구(CLI) 사용이 필요해 조금 더 어렵습니다.

어느 것이든 이 저장소의 `Dockerfile` 하나로 동작합니다. 진행 중 막히면 화면을 캡처해
보내주시면 바로 도와드리겠습니다.
